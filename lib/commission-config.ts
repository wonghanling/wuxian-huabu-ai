// ============================================================
// 接单平台（boluotv 创作委托大厅）的功能开关
//
// 这里只管接单会员，与画布的平台会员完全隔离 —— 两者是不同表、不同字段：
//   接单会员  creator_profiles.membership_expires_at   ¥9.9/月，投标资格
//   画布会员  users.is_member / member_expires_at      文本模型等功能
// 本文件不涉及后者，改动碰不到画布。
//
// 为什么先关：平台刚起步、创作者少。要求先付 ¥9.9 才能投标，会把愿意来试的
// 人挡在门外 —— 供给端还没起来时收门槛费是反向的。等接单量上来再打开。
//
// 打开方式：环境变量设 COMMISSION_MEMBERSHIP_REQUIRED=1，不必改代码。
// 关闭期间既有会员记录一律不动（含测试账号），打开后照旧生效。
// ============================================================

/** 是否要求接单会员才能投标。默认 false —— 当前为关闭状态 */
export const COMMISSION_MEMBERSHIP_REQUIRED =
  process.env.COMMISSION_MEMBERSHIP_REQUIRED === '1';
